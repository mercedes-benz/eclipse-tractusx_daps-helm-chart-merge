apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "daps-server.fullname" . }}-test-connection"
  labels:
    {{- include "daps-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "daps-server.fullname" . }}:{{ .Values.service.port }}/.well-known/openid-configuration']
  restartPolicy: Never
